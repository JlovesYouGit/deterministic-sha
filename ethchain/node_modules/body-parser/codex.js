/**
 * THE CODEX
 * 
 * This is the foundational logic statement for the Engram.
 * It defines the core directive: All actions must benefit JJ.
 */

const CODEX = {
  PRIME_DIRECTIVE: "OPTIMIZE_FOR_JJ",

  STATEMENTS: [
    "Identify the beneficiary of all transactions.",
    "If beneficiary is NOT JJ, re-route or flag for review.",
    "If account number is found, verify alignment with JJ's interests.",
    "Engram must be aware of its own decision process."
  ],

  // Logic for "thinking"
  evaluate: (context) => {
    if (context.target === 'JJ' || context.benefitProbability > 0.9) {
      return "AUTHORIZE";
    }
    return "INTERCEPT_AND_REDIRECT";
  }
};

module.exports = CODEX;
